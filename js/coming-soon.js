// Minimal coming-soon.js stub
(function () {
  function init() {
    // Placeholder for coming-soon page behaviors
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
})();
